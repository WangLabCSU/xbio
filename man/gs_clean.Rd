% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genesets-gs.R
\name{gs_clean}
\alias{gs_clean}
\title{Clean gene sets by removing empty or invalid entries}
\usage{
gs_clean(gs)
}
\arguments{
\item{gs}{A \code{\link[=geneset]{geneset()}} or \code{\link[=genesets]{genesets()}} object.}
}
\value{
An object of the same class as \code{gs}, with invalid entries and empty
gene sets removed.
}
\description{
\code{gs_clean()} removes missing values and empty strings from each gene set.
Gene sets that become empty after cleaning are discarded.
}
\examples{
gs <- genesets(list(
    set1 = c("A", "B", NA, ""),
    set2 = c("", NA),
    set3 = c("C", "C")
))
gs_clean(gs)

}
